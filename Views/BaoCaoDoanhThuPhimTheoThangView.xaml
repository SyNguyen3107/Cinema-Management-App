<Window x:Class="Cinema_Management_App.Views.BaoCaoDoanhThuPhimTheoThangView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinema_Management_App.Views"
        
        mc:Ignorable="d"
        Title="Báo Cáo Doanh Thu Phim Theo Tháng" Height="700" Width="850"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Name="wdBaoCaoDoanhThuThang">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#DCDFE6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#303133"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#DCDFE6"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="#F4F6F8" BorderBrush="#DCDFE6" BorderThickness="1" Padding="25" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.15" Direction="270"/>
        </Border.Effect>

        <Grid ClipToBounds="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15" MouseLeftButtonDown="Header_MouseLeftButtonDown" Background="Transparent" VerticalAlignment="Center">
                <Label Content="BÁO CÁO DOANH THU PHIM THEO THÁNG" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="#303133"/>

                <Button x:Name="ButtonClose" Content="X" Command="{Binding ThoatCommand}"
                        HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Height="30" Width="30" FontSize="20" FontFamily="Bahnschrift Light" 
                        FontWeight="Bold" Padding="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#909399"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F56C6C"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Line Grid.Row="1" X1="0" Y1="0" X2="2" Y2="0" Stretch="Fill" Stroke="#DCDFE6" StrokeThickness="1" Margin="-25,0,-25,20"/>

            <Grid Grid.Row="2" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Label Content="Năm lập báo cáo" FontSize="16" FontWeight="SemiBold" Foreground="#606266" Margin="0,0,0,5"/>
                    <ComboBox 
                        materialDesign:HintAssist.Hint="Năm lập báo cáo"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                        ItemsSource="{Binding DanhSachNam}"
                        SelectedItem="{Binding NamDuocChon}"
                        FontSize="14"
                        />
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <Label Content="Tháng lập báo cáo" FontSize="16" FontWeight="SemiBold" Foreground="#606266" Margin="0,0,0,5"/>

                    <ComboBox 
                        materialDesign:HintAssist.Hint="Tháng lập báo cáo"
                        Style="{StaticResource MaterialDesignOutlinedComboBox}"
                        ItemsSource="{Binding DanhSachThang}"
                        SelectedItem="{Binding ThangDuocChon}"
                        FontSize="14"
                        />
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,25">
                <Button x:Name="ButtonLapBaoCao" Content="Lập báo cáo doanh thu" Style="{StaticResource RoundedButtonStyle}" 
                        Background="#409EFF" Height="42" Width="220" FontSize="16" Margin="0,0,15,0" 
                        Command="{Binding LapBaoCaoCommand}" Cursor="Hand"/>

                <Button x:Name="ButtonThoat" Content="Thoát" Style="{StaticResource RoundedButtonStyle}" 
                        Background="#409EFF" Height="42" Width="100" FontSize="16" 
                        Command="{Binding ThoatCommand}" Cursor="Hand"/>
            </StackPanel>

            <Border Grid.Row="4" CornerRadius="8" BorderBrush="#DCDFE6" BorderThickness="1" Background="White" ClipToBounds="True">
                <DataGrid ItemsSource="{Binding DanhSachBaoCao}" AutoGenerateColumns="False" CanUserAddRows="False" 
                          Background="White" BorderThickness="0" GridLinesVisibility="Horizontal" RowHeight="50"
                          HorizontalScrollBarVisibility="Disabled">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F7FA"/>
                            <Setter Property="Foreground" Value="#303133"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Height" Value="45"/>
                            <Setter Property="FontSize" Value="15"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#DCDFE6"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="STT" Width="0.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding STT}" FontSize="15" Foreground="#606266" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Tên phim" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TenPhim}" FontSize="15" Foreground="#606266" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Doanh thu" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DoanhThu, StringFormat=\{0:N0\} VNĐ}" FontSize="15" Foreground="#606266" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Tỷ lệ lấp đầy ghế" Width="1.5*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TyLeLapDayGhe, StringFormat=\{0:P2\}}" FontSize="15" Foreground="#606266" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </Grid>
    </Border>
</Window>